object Example {

  /**
   * Check if the provided value is bigger than 10.
   *
   * @param i Provided integer value.
   * @return `true` if bigger than 10, `false` otherwise.
   */
  def biggerThan10(i: Int): Boolean = i > 10

}
